<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/p2p_room_booking_logo"
        android:label="@string/app_name"
        android:logo="@drawable/p2p_room_booking_logo"
        android:roundIcon="@drawable/p2p_room_booking_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.P2PRoomBooking"
        android:usesCleartextTraffic="true"

        >

        <activity android:name=".HomeActivity" />

        <activity
            android:name=".AuthActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".CreateBookingActivity" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EditBookingActivity" />
        <activity android:name=".ResolveConflictActivity" />

    </application>

</manifest>
