<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.P2PRoomBooking"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".booking.CreateBookingActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!-- HomeActivity ko Launcher bna diya -->
        <activity android:name=".home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".booking.EditBookingActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".auth.AuthActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".booking.BookingDetailActivity" />
        <activity
            android:name=".home.FilterActivity"
            android:theme="@style/Theme.P2PRoomBooking.FilterDialog" />

        <!-- SplashActivity se intent-filter hta diya -->
        <activity android:name=".common.SplashActivity" />

        <activity android:name=".security.UnlockCryptoActivity" />

    </application>
</manifest>
