<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_16"
    android:background="@drawable/bg_detail_card"
    android:orientation="vertical"
    android:padding="@dimen/space_12">

    <TextView
        android:id="@+id/tvGroupTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Beta - 12 Rooms"
        android:textColor="@color/detail_title"
        android:textSize="@dimen/text_17"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_12"
        android:orientation="horizontal">

        <TextView style="@style/CalendarWeekText" android:text="Sun" />
        <TextView style="@style/CalendarWeekText" android:text="Mon" />
        <TextView style="@style/CalendarWeekText" android:text="Tue" />
        <TextView style="@style/CalendarWeekText" android:text="Wed" />
        <TextView style="@style/CalendarWeekText" android:text="Thu" />
        <TextView style="@style/CalendarWeekText" android:text="Fri" />
        <TextView style="@style/CalendarWeekText" android:text="Sat" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvDays"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_8"
        android:nestedScrollingEnabled="false"
        android:overScrollMode="never"
        android:scrollbars="none" />

</LinearLayout>